<!-- @foreach($lsRole as $role)
@if($role->id == $staff->role)
<td value="{{$role->id}}">{{$role->role_name}}</td>
@endif
@endforeach -->


<!-- // comment search
<div class="header-content clearfix">
                        <div>
                            <div class="header-right">
                                <div class="input-group icons">
                                    <form type="get" action="{{route('admin.searchComment','lsComment')}}">
                                        <input type="search" name="query" class="form-control" placeholder="Search">
                                    </form>
                                    <div class="input-group-prepend">
                                        <button class="input-group-text bg-transparent border-0 pr-2 pr-sm-3"
                                            id="basic-addon1"><i class="mdi mdi-magnify"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    /// -->
<!-- {{route('admin.personalInfo','lsAdmin')}} -->



<!-- <div class="header-right" style="padding:200px">
    <div class="row">
        <div class="col-lg-3">
            <p>Main Image</p>
            <form action="/delete-img/{{$Pro->id}}" method="post">
                <button class="btn text-danger">X </button>
                @csrf
                @method('delete')
            </form>
            <img src="/image/{{$Pro->image}}" alt="" class="img-reponsive" style="max-height:100px; max-width: 100px;"
                srcset="">
            <br>

            <p>Main Image</p>

            @foreach($Pro->pictures as $pic)

            <form action="/delete-img/{{$pic->id}}" method="post">
                <button class="btn text-danger">X </button>
                @csrf
                @method('delete')
            </form>
            <img src="/pictures/{{$pic->link}}" alt="" class="img-reponsive" style="max-height:100px; max-width: 100px;"
                srcset="">

            @endforeach
        </div>
    </div>
</div> -->